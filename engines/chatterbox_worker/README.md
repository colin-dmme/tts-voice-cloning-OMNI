# Chatterbox Worker

Isolated worker for ResembleAI Chatterbox Turbo. Install from the project root with:

```bat
install_chatterbox_worker.bat
```

For CUDA on Windows, use:

```bat
install_chatterbox_worker_cuda.bat
```

The installer keeps `chatterbox-tts` at the newest compatible PyPI release allowed by this worker.
