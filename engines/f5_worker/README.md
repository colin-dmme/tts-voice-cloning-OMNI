# F5-TTS Worker

Isolated worker for SWivid/F5-TTS. Install from the project root with:

```bat
install_f5_worker.bat
```

For CUDA:

```bat
install_f5_worker_cuda.bat
```

This worker is used only when the selected model provider is `f5tts`.
