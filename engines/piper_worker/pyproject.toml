[project]
name = "omni-piper-worker"
version = "0.1.0"
description = "Isolated Piper ONNX worker for fixed Vietnamese voices."
requires-python = ">=3.10,<3.14"
dependencies = [
    "piper-tts>=1.6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []

[tool.uv]
package = false
